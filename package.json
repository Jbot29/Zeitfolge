{
  "name": "zeitfolge",
  "version": "0.1.0",
  "private": true,
  "description": "A tiny language for time — UTC underneath, civil time as a lens",
  "scripts": {
    "test": "node --test tests/",
    "serve": "python3 -m http.server 8000"
  }
}
